/*
#include<stdio.h>
struct student
{
  int roll;
  int mark;
};

int main()
{
  char result (struct student);
  struct student s;
  char z;
  printf ("enter roll and mark \n");
  scanf ("%d %d", &s.roll, &s.mark);
  z=result(s);
  printf("%c",z);
  return 0;
}

char result (struct student t)
{
  if (t.mark >= 50)
  {
    return ('P');
  }
  else 
  {
    return('F');
  }
}
*/
/// WAP to add two distances (in inch-feet) using structures.///
#include <stdio.h>
struct distance
{
  int dist1;
  int dist2;
};

int main()
{
  char result (struct distance);
  struct 
}