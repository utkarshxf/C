#include <stdio.h>

int main()
{
  int n;
  printf("how many student are there \n");
  scanf(" %d", &n);
  char student[n - 1][10] for (int i = 0; i < n - 1; i++)
  {
    int b = i + 1 printf("enter name of student name %d", b);
    scanf("%s", &student[i][]);
  }

  return 0;
}
